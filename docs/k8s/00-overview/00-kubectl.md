---
sidebar_position: 1
---

# The kubectl command-line tool

Command line tool for commuticating with a Kubenetes cluster's control plane, usign a Kurbernetes API

kube is the primary command-line tool for communicating with a Kubernetes cluster.

## Role of kubectl

The `kubectl` tool is the primary interface for creating, inspecting, updating, and deleting Kubernetes objects.