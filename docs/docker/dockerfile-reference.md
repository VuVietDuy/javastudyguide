---
sidebar_position: 2
---

# Dockerfile reference

Docker can build images automatically by reading the instructions form a Dockerfile. A Dockerfile is a text document that contains all the commands a user could call on the command line to assemble an image.

- ADD
- ARG
- CMD
